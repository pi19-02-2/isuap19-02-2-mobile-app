&НаКлиенте
Процедура Войти(Команда)
	Если ЭтаФорма.Email = "" ИЛИ ЭтаФорма.Пароль = "" Тогда
		Сообщить("Email и пароль должны быть заполнены.");
	КонецЕсли;           
	   
	ИзменениеКонстант(Email, Пароль); 		
КонецПроцедуры        


&НаСервере
Функция ИзменениеКонстант(Email, Пароль) 
	
	 Константы.EmailПользователя.Установить(Email) ;
	 Константы.ПарольПользователя.Установить(Пароль); 
	 
КонецФункции	