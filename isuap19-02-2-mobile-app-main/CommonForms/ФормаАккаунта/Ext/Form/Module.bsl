
&НаСервере
Процедура ВыйтиНаСервере()

	Док = Документы.Заказы.Выбрать();
	 Пока Док.Следующий() цикл
		 ОбъектДок = Док.ПолучитьОбъект();
		 ОбъектДок.Удалить();
	 КонецЦикла;  
	 
	 
	 
	 Константы.EmailПользователя.Установить("") ;
	 Константы.ПарольПользователя.Установить(""); 
	 Константы.Корзина.Установить(""); 
	
	 
КонецПроцедуры

&НаКлиенте
Процедура Выйти(Команда)
	
	Оповестить("НаименованиеСобытия"); 
	ВыйтиНаСервере();
	
КонецПроцедуры
